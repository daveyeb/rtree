
pub struct Token {
    // pub kind: BufferType,
    pub lexeme: String,
    pub raw: String,
}

// impl Buffer for Token {
//     fn btype(&self) -> &BufferType {
//         &self.kind
//     }
// }
